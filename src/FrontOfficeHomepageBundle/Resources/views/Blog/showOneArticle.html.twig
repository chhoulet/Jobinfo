{% extends 'FrontOfficeHomepageBundle::layout.html.twig' %}

	{% block content %}

		<div class="col-lg-12 text-center">
            <img class="img-responsive img-border img-full" src="img/slide-1.jpg" alt="">
                <h2>{{ showOneArticle.title }}
                    <br>
                    <small>{{ showOneArticle.subject }} <br>
                           {{ showOneArticle.dateCreated | date('d-m-Y H:i:s') }}</small>
                </h2>
                    <p>{{ showOneArticle.content }}</p>
                    <hr>
        </div>

        <div class="col-lg-12 text-center">

        		<h2>Ajoutez un commentaire :</h2>
        			{{ form(formComment) }}
        		
        </div>

        <div class="col-lg-12 text-center">

        		{# <a href="{{ path('front_office_homepage_createComment',{id:showOneArticle.id})}}">
					<h2>Commentez cet article :</h2></a> #}

        		{# {{ render(controller('FrontOfficeHomepageBundle:Comment:showComments.html.twig', {id:showOneArticle.id})) }} #}

        </div>

	{% endblock %}
{% extends 'BackOfficeBundle::layout.html.twig' %}

    {% block content %}

		<h2> Liste des forums : </h2>

            {% for message in app.session.flashbag.get('forum') %}
                {{ message }}
            {% endfor %}

            {% for messag in app.session.flashbag.get('edit') %}
                {{ messag }}
            {% endfor %}

            {% for messa in app.session.flashbag.get('notice') %}
                {{ messa }}
            {% endfor %}

    		{% for forum in showForums %}

        		<div id="top" class="row">
                    <div class="box">
                        <div class="col-lg-12">
                            <hr>
                            <h2 class="intro-text text-center"><strong>{{ forum.forumName }}</strong><br>
                                {{forum.forumType}}
                            </h2>
                            <hr>
                            <p>Date : {{forum.forumDate | date('d-m-Y H:i:s') }}</p>
                            <p style="text-align:justify;">{{ forum.forumDescription}}</p>
                        </div>
                        <a href="{{ path('back_office_adminforum_edit',{id:forum.id}) }}"><button>Mise à jour :</button></a>
                        <a href="{{ path('back_office_adminforum_showOne',{id:forum.id}) }}"><button>Edition :</button></a>
                        <hr>
                    </div>
                </div>
                
        	{% endfor %}

            <a href="#top"><button>Retour en haut de page</button></a>

	{% endblock %}